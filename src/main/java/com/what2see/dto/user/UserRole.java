package com.what2see.dto.user;

/**
 * Utility enum for communicating user roles with the client.<br>
 * Note that the ordinal of each value must match the client side.
 */
public enum UserRole {
    TOURIST, GUIDE, ADMINISTRATOR
}
